# 포트폴리오 사이트

정보보안 엔지니어 송지연의 포트폴리오 웹사이트입니다.

## 기능

- 📄 프로필 섹션
- 💼 희망직무 섹션
- 🎓 직무 자격사항 섹션
- 🏫 학력(Education) 섹션
- 🚀 프로젝트 섹션 (PDF 다운로드 지원)
- 📱 반응형 디자인
- 🎨 모던한 UI (글래스모피즘, 그라데이션)

## 커스터마이징 방법

모든 내용은 `config.json` 파일을 수정하여 변경할 수 있습니다.

### 1. 프로필 정보 수정

```json
{
  "profile": {
    "name": "송지연",
    "title": "정보보안 엔지니어",
    "photo": "assets/images/profile.jpg",  // 프로필 사진 경로 (선택사항)
    "email": "your.email@example.com",
    "phone": "010-1234-5678",
    "location": "서울, 대한민국",
    "bio": "자기소개를 여기에 작성하세요.",
    "social": {
      "github": "https://github.com/yourusername",
      "linkedin": "https://linkedin.com/in/yourusername",
      "blog": "https://yourblog.com"
    }
  }
}
```

### 2. 희망직무 정보 수정

```json
{
  "desiredPosition": {
    "title": "정보보안 엔지니어",
    "description": "상세한 희망직무 설명을 작성하세요.",
    "focus": "클라우드 보안"
  }
}
```

### 3. 자격사항 추가

```json
{
  "qualifications": [
    {
      "name": "자격증 이름",
      "issuer": "발급기관",
      "date": "2024-01-01",
      "number": "자격번호 (선택사항)"
    }
  ]
}
```

### 4. 학력 정보 추가

```json
{
  "education": [
    {
      "school": "학교명",
      "major": "전공",
      "degree": "학위 (예: 학사, 석사)",
      "period": "기간 (예: 2020-2024)",
      "gpa": "GPA (선택사항)"
    }
  ]
}
```

### 5. 프로젝트 추가

```json
{
  "projects": [
    {
      "title": "프로젝트 제목",
      "description": "프로젝트 설명",
      "tech": ["기술1", "기술2", "기술3"],
      "pdf": "assets/pdfs/project1.pdf",
      "github": "https://github.com/username/project",
      "demo": "https://demo-url.com"
    }
  ]
}
```

**PDF 파일 추가 방법:**
1. `assets/pdfs/` 폴더에 PDF 파일을 저장합니다.
2. `config.json`의 `pdf` 필드에 경로를 입력합니다 (예: `"assets/pdfs/myproject.pdf"`).

### 6. 섹션 추가/제거

`sections` 배열에서 원하는 섹션을 추가하거나 제거할 수 있습니다:

```json
{
  "sections": [
    "profile",
    "desiredPosition",
    "qualifications",
    "education",
    "projects"
  ]
}
```

## 로컬에서 실행하기

1. 이 저장소를 클론하거나 다운로드합니다.
2. 웹 서버를 실행합니다:
   - Python: `python -m http.server 8000`
   - Node.js: `npx serve`
   - VS Code: Live Server 확장 사용
3. 브라우저에서 `http://localhost:8000` 접속

## GitHub Pages에 배포하기

1. GitHub에 저장소를 푸시합니다.
2. 저장소 설정 > Pages로 이동합니다.
3. Source를 "Deploy from a branch"로 선택합니다.
4. Branch를 "main" (또는 "master")로 선택하고 `/ (root)`를 선택합니다.
5. 저장 후 몇 분 후 `https://yourusername.github.io/repository-name`에서 확인할 수 있습니다.

## 파일 구조

```
github.myio/
├── index.html              # 메인 HTML 파일
├── config.json            # 포트폴리오 설정 파일
├── assets/
│   ├── css/
│   │   └── style.css      # 스타일시트
│   ├── js/
│   │   └── main.js        # JavaScript 로직
│   └── pdfs/              # 프로젝트 PDF 파일
└── README.md              # 이 파일
```

## 스타일 커스터마이징

`assets/css/style.css` 파일의 CSS 변수를 수정하여 색상 테마를 변경할 수 있습니다:

```css
:root {
    --primary-color: #6366f1;
    --secondary-color: #8b5cf6;
    --accent-color: #ec4899;
    /* ... */
}
```

## 라이선스

이 프로젝트는 자유롭게 사용하고 수정할 수 있습니다.

